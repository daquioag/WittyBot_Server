export type LoginUserParams = {
    email: string;
    password: string;
    // username: string;
    // apicalls: number;
    // token?: string;
    // admin: boolean;
    // Add other user properties as needed
  };