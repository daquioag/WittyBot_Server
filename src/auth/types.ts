export type LoginUserParams = {
    username: string;
    password: string;
    // email: string;
    // apicalls: number;
    // token?: string;
    // admin: boolean;
    // Add other user properties as needed
  };